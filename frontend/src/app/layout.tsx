import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "IncomeOS — Your AI-Powered Income Operating System",
  description:
    "Diagnose your skills, discover monetization paths, create artifacts, automate workflows, and monitor revenue — all in one AI-driven platform.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
